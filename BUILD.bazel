load("@aspect_rules_ts//ts:defs.bzl", "ts_config")

package(default_visibility = ["//:__subpackages__"])

exports_files([
    "tsconfig.json",
])

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
    deps = [],
)
